"use client";

import { TeamServices } from "@/services/teams";

const TeamPage = ({ params }) => {
  const fetchData = async () => {
    try {
      // const data = TeamServices.fetchById(params.teamId);
    } catch (error) {}
  };
  return "hello";
};

export default TeamPage;
